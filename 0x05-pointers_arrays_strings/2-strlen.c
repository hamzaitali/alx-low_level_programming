#include "main.h"
#include<string.h>

/**
 * _strlen - return the length of a string
 * Description: return lenght of a string
 * @c : the charactere received
 *
 * Return: Always int
 **/

int _strlen(char *c)
{
	int i = strlen(c);

	return (i);
}
